import { httpClient } from '../../../shared/utils/httpClient.js';

export class LandingService {
  // Service implementation if landing requires data
}

export const landingService = new LandingService();
